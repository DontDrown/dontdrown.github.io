function getTooltip() {
    // Tooltip needs to be returned in form of html property of object
    return (
      {
        html: `<p>Placeholder ToolTip</p>`
      }
    );
  }

export default getTooltip